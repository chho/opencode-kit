---
description: Smart commit - analyze diff, group files, and commit with conventional messages
agent: build
---

Load the skill "smart-commit" first, then follow its workflow strictly.

Here is the current git status:

!`git status`

Here is the current unstaged diff:

!`git diff`

Here is the current staged diff:

!`git diff --cached`

Now analyze the above changes and execute the smart-commit workflow as defined in the skill.
